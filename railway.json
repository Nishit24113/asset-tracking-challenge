{
  "$schema": "https://railway.com/reference/config-as-code/schema.json",
  "build": {
    "dockerfilePath": "api/Dockerfile",
    "dockerContext": "api",
    "watchPatterns": ["api/**"]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
